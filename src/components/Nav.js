import React from 'react';

const Nav = props => {
  return (
    <div
      className="navigation"
      style={{
        margin: "auto 1em"
      }}>
      <i className="material-icons">menu</i>
    </div>
  );
};

export default Nav;
