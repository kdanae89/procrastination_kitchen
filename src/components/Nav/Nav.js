import React from 'react';
import './Nav.scss';

const Nav = props => {
  return (
    <div className="navigation">
      <i className="material-icons">menu</i>
    </div>
  );
};

export default Nav;
